extends layout

block content
  .main.container
    button.btn.btn-primary(onclick="window.location.href = '/auth/logout'") Logout
    button.btn.btn-primary(onclick="window.location.href = '/feed/newPublication'") Create New Publication
    h1 Feed
    each p in feed
      h3= p.title
      div= p.text
      h3 Author:
      div= p.author
      each c in p.comments
        h3 Comment:
        div= c.commentAuthor 
        div= c.commentText
      h3 ------------------------------------------------------------